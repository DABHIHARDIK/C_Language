#include<stdio.h> 
#include<string.h>

char* reverseWords(char* s) {
    int start = 0 , len = strlen(s);
    while(len != 0 ){
        
    }    
}

int main(){
    char s[] = "  hello world  ";
    reverseWords(s);
    return 0;
}