/* 
0> what is preprocessor?????
    pre-processor is a program which performs before the compilation
    pre-processor only notices # started statements 
    # called pre-processor directive
    Each preprocessign directive must be on its own line
    The word after # is called pre-processor command

0> #include
#include<stdio.h>
#include"string.h"
#include"c:/hardik/program/test.c"
*/

